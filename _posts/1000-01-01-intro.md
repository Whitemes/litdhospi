<style>
  .intro {
    position: relative;
    height: 100%;
    width: 100%;
    display: grid;
    place-items: center;
    overflow: hidden;
    color: #fff;
    font-family: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  }

  /* Soft ambient background */
  .intro::before {
    content: "";
    position: absolute;
    inset: -20%;
    background:
      radial-gradient(700px 500px at 50% 45%, rgba(56,189,248,.18), transparent 65%),
      radial-gradient(800px 600px at 70% 70%, rgba(99,102,241,.12), transparent 70%);
    filter: blur(30px);
    z-index: 0;
  }

  /* Subtle tech grid */
  .intro::after {
    content: "";
    position: absolute;
    inset: 0;
    background-image:
      linear-gradient(rgba(255,255,255,.03) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,.03) 1px, transparent 1px);
    background-size: 80px 80px;
    mask-image: radial-gradient(ellipse at center, black 40%, transparent 85%);
    -webkit-mask-image: radial-gradient(ellipse at center, black 40%, transparent 85%);
    z-index: 0;
  }

  .hero {
    position: relative;
    z-index: 2;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 22px;
    padding: 40px 20px;
  }

  /* ESIEE logo – blended, no box */
  .school-logo {
    height: 54px;
    width: auto;
    opacity: 0.75;
    filter: brightness(1.05);
  }

  .kicker {
    margin: 0;
    font-size: 1.05rem;
    letter-spacing: .35em;
    text-transform: uppercase;
    color: rgba(255,255,255,.65);
    font-weight: 600;
  }

  .title {
    margin: 0;
    font-size: clamp(3.2rem, 7vw, 6.4rem);
    font-weight: 900;
    letter-spacing: -0.04em;
    text-transform: uppercase;
    background: linear-gradient(180deg, #ffffff 0%, #9be7ff 55%, #38bdf8 120%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
  }

  .subtitle {
    margin: 6px 0 0;
    font-size: clamp(1.05rem, 2.2vw, 1.5rem);
    color: rgba(255,255,255,.82);
    font-weight: 350;
    position: relative;
    padding-top: 18px;
  }

  .subtitle::before {
    content: "";
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 90px;
    height: 2px;
    background: linear-gradient(90deg, transparent, rgba(56,189,248,.9), transparent);
  }

  @media (max-width: 768px) {
    .school-logo { height: 44px; }
    .kicker { letter-spacing: .25em; }
  }
</style>

<div class="intro">
  <div class="hero">
    <img
      src="./images/esiee-eiffel.jpg"
      alt="Université Gustave Eiffel – ESIEE Paris"
      class="school-logo"
    >

    <p class="kicker">Last Project</p>
    <h1 class="title">LITDHOSPI</h1>
    <p class="subtitle">Présentation des outils</p>
  </div>
</div>
