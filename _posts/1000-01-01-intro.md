<style>
  .intro {
    position: relative;
    height: 100%;
    width: 100%;
    display: grid;
    place-items: center;
    overflow: hidden;
    color: #fff;
    font-family: Inter, -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
  }

  /* Soft glow */
  .intro::before{
    content:"";
    position:absolute;
    inset:-20%;
    background:
      radial-gradient(600px 400px at 50% 40%, rgba(56,189,248,.18), transparent 60%),
      radial-gradient(700px 500px at 70% 70%, rgba(99,102,241,.12), transparent 65%);
    filter: blur(20px);
    pointer-events:none;
    z-index:0;
  }

  /* Subtle grid */
  .intro::after{
    content:"";
    position:absolute;
    inset:0;
    background-image:
      linear-gradient(rgba(255,255,255,.035) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255,255,255,.035) 1px, transparent 1px);
    background-size: 70px 70px;
    opacity: .55;
    mask-image: radial-gradient(ellipse at center, black 35%, transparent 80%);
    -webkit-mask-image: radial-gradient(ellipse at center, black 35%, transparent 80%);
    pointer-events:none;
    z-index:0;
  }

  /* Glass panel */
  .intro-card{
    position: relative;
    z-index: 2;
    width: min(980px, calc(100% - 56px));
    padding: 44px 46px 38px;
    border-radius: 22px;
    background: rgba(255,255,255,.055);
    border: 1px solid rgba(255,255,255,.12);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    box-shadow:
      0 18px 60px rgba(0,0,0,.45),
      inset 0 1px 0 rgba(255,255,255,.08);
  }

  /* Top row: badges */
  .intro-top{
    display:flex;
    align-items:center;
    justify-content:space-between;
    gap: 16px;
    margin-bottom: 26px;
  }

  .logo-pill{
    display:inline-flex;
    align-items:center;
    justify-content:center;
    padding: 10px 12px;
    border-radius: 12px;
    background: rgba(0,0,0,.35);
    border: 1px solid rgba(255,255,255,.14);
    box-shadow: 0 10px 30px rgba(0,0,0,.35);
  }

  .logo-pill img{
    display:block;
    height: 38px;
    width: auto;
    max-width: 220px;
    object-fit: contain;
    filter: drop-shadow(0 0 10px rgba(56,189,248,.12));
  }

  /* Center content */
  .intro-hero{
    text-align:center;
    padding: 6px 0 2px;
  }

  .kicker{
    margin: 0 0 14px;
    font-size: 1.05rem;
    text-transform: uppercase;
    letter-spacing: .28em;
    color: rgba(255,255,255,.72);
    font-weight: 650;
  }

  .title{
    margin: 0;
    font-size: clamp(3rem, 7vw, 6.2rem);
    line-height: .95;
    letter-spacing: -0.04em;
    font-weight: 900;
    text-transform: uppercase;
    background: linear-gradient(180deg, #ffffff 0%, #9be7ff 55%, #38bdf8 115%);
    -webkit-background-clip: text;
    background-clip: text;
    color: transparent;
    filter: drop-shadow(0 0 42px rgba(56,189,248,.22));
  }

  .subtitle{
    margin: 18px 0 0;
    font-size: clamp(1.05rem, 2.2vw, 1.55rem);
    color: rgba(255,255,255,.86);
    font-weight: 350;
    letter-spacing: .02em;
    position: relative;
    display: inline-block;
    padding-top: 18px;
  }

  .subtitle::before{
    content:"";
    position:absolute;
    top:0;
    left:50%;
    transform: translateX(-50%);
    width: 92px;
    height: 2px;
    border-radius: 2px;
    background: linear-gradient(90deg, transparent, rgba(56,189,248,.95), transparent);
    opacity: .9;
  }

  @media (max-width: 768px){
    .intro-card{
      padding: 28px 22px 26px;
      width: calc(100% - 28px);
      border-radius: 18px;
    }
    .intro-top{
      flex-direction: column;
      align-items: center;
      margin-bottom: 18px;
    }
    .logo-pill img{
      height: 34px;
      max-width: 240px;
    }
    .kicker{ letter-spacing: .22em; }
  }
</style>

<div class="intro">
  <div class="intro-card">
    <div class="intro-top">
      <div class="logo-pill">
        <img src="./images/esiee-eiffel.jpg" alt="Université Gustave Eiffel / ESIEE Paris">
      </div>
      <div class="logo-pill">
        <img src="./images/litdhospi.png" alt="LITDHOSPI">
      </div>
    </div>

    <div class="intro-hero">
      <p class="kicker">Last Project</p>
      <h1 class="title">LITDHOSPI</h1>
      <p class="subtitle">Présentation des outils</p>
    </div>
  </div>
</div>
