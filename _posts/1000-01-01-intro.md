---
layout: post
---


<style>
  .intro-container {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100%;
    width: 100%;
    position: relative;
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif;
    overflow: hidden;
  }

  /* Background ambiance via pseudo-element */
  .intro-container::before {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 60vw;
    height: 60vw;
    background: radial-gradient(circle, rgba(79, 172, 254, 0.1) 0%, rgba(0, 0, 0, 0) 70%);
    border-radius: 50%;
    filter: blur(60px);
    z-index: 0;
    pointer-events: none;
  }

  /* Grid pattern overlay (optional depth) */
  .intro-container::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background-image: linear-gradient(rgba(255, 255, 255, 0.03) 1px, transparent 1px),
    linear-gradient(90deg, rgba(255, 255, 255, 0.03) 1px, transparent 1px);
    background-size: 50px 50px;
    z-index: 0;
    pointer-events: none;
    mask-image: radial-gradient(circle at center, black 40%, transparent 100%);
    -webkit-mask-image: radial-gradient(circle at center, black 40%, transparent 100%);
  }

  .header-logos {
    position: absolute;
    top: 40px;
    left: 40px;
    z-index: 2;
  }

  .school-logo {
    height: 60px;
    width: auto;
    opacity: 0.9;
    filter: drop-shadow(0 2px 4px rgba(0,0,0,0.5));
    transition: opacity 0.3s ease;
  }
  .school-logo:hover {
    opacity: 1;
  }

  .hero-content {
    z-index: 2;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 10px;
  }

  .project-logo-center {
    width: 120px;
    height: auto;
    margin-bottom: 20px;
    filter: drop-shadow(0 0 15px rgba(79, 172, 254, 0.4));
    animation: float 6s ease-in-out infinite;
  }

  .sur-title {
    font-size: 1.2rem;
    letter-spacing: 4px;
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.6);
    margin: 0;
    font-weight: 400;
  }

  .project-title {
    font-size: 6rem;
    margin: 10px 0;
    font-weight: 800;
    letter-spacing: -2px;
    line-height: 1.1;
    background: linear-gradient(135deg, #a1c4fd 0%, #c2e9fb 50%, #d4fc79 100%); /* Blue to green/cyan gradient */
    background: linear-gradient(90deg, #4facfe 0%, #00f2fe 100%); /* Fallback / Primary requested gradient */
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    filter: drop-shadow(0 0 25px rgba(79, 172, 254, 0.4));
    text-transform: uppercase;
  }

  .subtitle {
    font-size: 1.5rem;
    color: rgba(255, 255, 255, 0.85);
    margin-top: 10px;
    font-weight: 300;
    letter-spacing: 0.5px;
    border-top: 1px solid rgba(255,255,255,0.2);
    padding-top: 20px;
    width: 80%;
    max-width: 500px;
  }

  @keyframes float {
    0% { transform: translateY(0px); }
    50% { transform: translateY(-10px); }
    100% { transform: translateY(0px); }
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .project-title { font-size: 3.5rem; }
    .school-logo { height: 40px; top: 20px; left: 20px; }
    .header-logos { top: 20px; left: 20px; }
  }
</style>

<div class="intro-container">
  <div class="header-logos">
    <img src="./images/esiee-eiffel.png" alt="ESIEE Paris Logo" class="school-logo">
  </div>

  <div class="hero-content">
    <img src="./images/litdhospi-logo.png" alt="LITDHOSPI Logo" class="project-logo-center">
    <h3 class="sur-title">LAST PROJECT</h3>
    <h1 class="project-title">LITDHOSPI</h1>
    <p class="subtitle">Présentation des outils & architecture</p>
  </div>
</div>
