
<style>
  .intro-container {
    display: flex;
    flex-direction: column;
    justify-content: center;
    align-items: center;
    height: 100%;
    width: 100%;
    position: relative;
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif;
    overflow: hidden;
    color: white;
  }

  /* Cinematic Background: Dark with subtle deep blue/purple glow */
  .intro-container::before {
    content: "";
    position: absolute;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 100vw;
    height: 100vw;
    background: radial-gradient(circle, rgba(56, 189, 248, 0.08) 0%, rgba(0, 0, 0, 0) 60%);
    border-radius: 50%;
    filter: blur(100px);
    z-index: 0;
    pointer-events: none;
  }

  /* Technical Grid Overlay */
  .intro-container::after {
    content: "";
    position: absolute;
    inset: 0;
    background-image: 
      linear-gradient(rgba(255, 255, 255, 0.02) 1px, transparent 1px),
      linear-gradient(90deg, rgba(255, 255, 255, 0.02) 1px, transparent 1px);
    background-size: 60px 60px;
    z-index: 0;
    pointer-events: none;
    mask-image: radial-gradient(ellipse at center, black 40%, transparent 80%);
    -webkit-mask-image: radial-gradient(ellipse at center, black 40%, transparent 80%);
  }

  /* Header Layout */
  .header-logos {
    position: absolute;
    top: 40px;
    left: 40px;
    right: 40px;
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    z-index: 2;
  }

  /* Shared Logo Style - "Sticker" look */
  .logo-badge {
    height: 45px; /* Refined size */
    width: auto;
    background: white;
    padding: 8px 12px;
    border-radius: 6px;
    box-shadow: 0 4px 15px rgba(0,0,0,0.5); /* Deep shadow for pop */
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  
  .logo-badge:hover {
    transform: translateY(-2px);
    box-shadow: 0 6px 20px rgba(0,0,0,0.6);
  }

  /* Hero Content Centered */
  .hero-content {
    z-index: 2;
    text-align: center;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 15px;
    transform: translateY(-10px);
  }

  /* Sur-title "LAST PROJECT" */
  .sur-title {
    font-size: 1.1rem;
    letter-spacing: 0.5em; /* Wide tracking */
    text-transform: uppercase;
    color: rgba(255, 255, 255, 0.6);
    margin: 0;
    font-weight: 600;
    text-shadow: 0 2px 4px rgba(0,0,0,0.5);
  }

  /* Main Title "LITDHOSPI" */
  .project-title {
    font-size: 7rem; /* Massive impact */
    margin: 10px 0;
    font-weight: 800;
    letter-spacing: -3px;
    line-height: 1;
    /* Clean, bright gradient */
    background: linear-gradient(180deg, #ffffff 10%, #6dd5fa 100%); 
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    filter: drop-shadow(0 0 50px rgba(56, 189, 248, 0.25)); /* Glow */
    text-transform: uppercase;
  }

  /* Subtitle */
  .subtitle {
    font-size: 1.5rem;
    color: rgba(255, 255, 255, 0.8);
    margin: 0;
    font-weight: 300;
    letter-spacing: 1px;
    position: relative;
    padding-top: 25px;
  }
  
  /* Modern separator line */
  .subtitle::before {
    content: "";
    position: absolute;
    top: 0;
    left: 50%;
    transform: translateX(-50%);
    width: 60px;
    height: 3px;
    background: linear-gradient(90deg, transparent, #38bdf8, transparent);
    border-radius: 2px;
  }

  /* Responsive */
  @media (max-width: 768px) {
    .project-title { font-size: 3.5rem; }
    .header-logos { top: 20px; left: 20px; right: 20px; }
    .logo-badge { height: 35px; }
    .subtitle { font-size: 1.1rem; }
  }
</style>

<div class="intro-container">
  <!-- Header: Logos anchored Left and Right -->
  <div class="header-logos">
    <!-- School Logo (Left) -->
    <img src="./images/esiee-eiffel.png" alt="ESIEE Paris Logo" class="logo-badge">
    
    <!-- Project Logo (Right) - Matching size/style -->
    <img src="./images/litdhospi-logo.png" alt="LITDHOSPI Logo" class="logo-badge">
  </div>

  <!-- Center: Text Only for maximum impact -->
  <div class="hero-content">
    <h3 class="sur-title">Last Project</h3>
    <h1 class="project-title">LITDHOSPI</h1>
    <p class="subtitle">Présentation des outils & architecture</p>
  </div>
</div>
